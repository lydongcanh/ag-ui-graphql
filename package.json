{
  "name": "ag-ui-graphql",
  "version": "0.0.0",
  "private": true,
  "description": "GraphQL transport bridge for AG-UI agent applications.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^22.13.0",
    "tsup": "^8.3.6",
    "turbo": "^2.5.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
